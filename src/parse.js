'use strict'

import _ from './lodash'

import operators from './operators'

module.exports = function(service) {
  return function parse(query) {
    var levels = []


  }
}

function go(query, levels){
  if(_.isArray(query)){

  }
}
